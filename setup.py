from setuptools import setup, find_packages

setup(name='pce_pinns',
      description='PCE PINNs',
      author='',
      author_email='',
      version='0.0.1',
      install_requires=[''],
      packages=find_packages(),
)